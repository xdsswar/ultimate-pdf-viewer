plugins {
    id 'java'
}

group 'xss.it.ultimate'

allprojects {
    version = '1.0.0'
}

subprojects {
    repositories {
        mavenCentral()
    }
}

tasks.register('setupEnv') {
    group = 'build setup'
    description = 'Sets up the local dev environment: native toolchain check + PDFium vendoring (-Pall for all platforms).'
    dependsOn ':nfx-pdfium:setupEnv'
}

tasks.register('buildAll') {
    group = 'build'
    description = 'Builds the native PDFium shim and assembles every module.'
    dependsOn ':nfx-pdfium:buildAll', ':pdf-viewer:assemble', ':demo:assemble'
}